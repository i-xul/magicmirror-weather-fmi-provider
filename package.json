{
  "name": "magicmirror-weather-fmi-provider",
  "version": "0.1.0",
  "description": "Finnish Meteorological Institute (FMI) weather provider for MagicMirror²",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "test": "node --test test/*.test.js",
    "test:bundle": "npm run build && node test/bundle-smoke.cjs",
    "test:live": "node --test test/fmi-live.integration.js",
    "check": "npm test && npm run test:bundle"
  },
  "keywords": [
    "magicmirror",
    "magicmirror2",
    "weather",
    "fmi",
    "finland"
  ],
  "author": "H A (i-xul)",
  "license": "MIT",
  "engines": {
    "node": ">=22.22.2 <23 || >=24"
  },
  "dependencies": {
    "fast-xml-parser": "^5.11.1",
    "sunrise-sunset-js": "^3.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
